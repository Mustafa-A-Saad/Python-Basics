import numpy as np

"""#Basics
a = np.array([1,2,3,4])
print(a)

a = a + 2 # you can use arithmetics ( + - * / )
print(a) # applies to every element in the array

b = np.array([[1,0,1,0]])
print(a+b) # you can add to another np.array

print(b**3)

print(np.cos(a)) # can use Trigs
print(np.sqrt(a)) # sqrt
print(np.std(a)) # Standard Deviation 

"""

"""#Linear Algebra # rules u studied apply
a = np.full((2,3),1) # fulls the matrix shape with the fill value
print(a)

b = np.full((3,2),2)
print(b)

print(np.matmul(a,b)) # must use the function matmul to multiply matrices

c = np.identity(3) # determinant of identity is 1
print(c)
print(np.linalg.det(c)) # finding the determinant
print(a.dot(b)) # dot product of 2 matrices
"""

# https://numpy.org/doc/stable/reference/routines.linalg.html
# VERY IMP!!! Try it
# Traces, Dot products , singular vector Decompsoition , inverse , Eigen Values,etc...







